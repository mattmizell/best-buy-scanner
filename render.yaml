services:
  - type: web
    name: best-buy-scanner
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn best_buy.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    healthCheckPath: /health
